% ---------------------------------------- BINARY ----------------------------------------

function bin = getBinary
    bin.and = @bin_and;
    bin.or = @bin_or;
    bin.xor = @bin_xor;
end

% bitwise AND
function bin = bin_and(a,b)
end

% bitwise OR
function bin = bin_or(a,b)
end

% bitwise XOR
function bin = bin_xor(a,b)
end